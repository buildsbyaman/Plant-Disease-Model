# 1. Install Project Dependencies (Specific versions for Python 3.9 stability)
pip3 install -r requirements.txt
pip3 install gradio==4.44.1 gradio-client==1.3.0 huggingface-hub==0.24.7 pydantic==2.8.2 --user --force-reinstall

# 2. Train the Plant Disease Detection Model (Task 1)
# Using DeiT Architecture + Focal Loss for imbalance
python3 train_plant.py

# 3. Train the Cassava Disease Detection Model (Task 2)
# Using Swin Transformer + Label Smoothing
python3 train_cassava.py

# 4. Launch the Interactive Dashboard (Inference UI + Metrics)
# Accessibility enabled with share=True
python3 test.py
